import React from "react";
import RouterConfig from "./routes/route";

const App: React.FC = () => {
  return (
    <React.Fragment>
      <RouterConfig />
    </React.Fragment>
  );
};

export default App;
